{
  "name": "agent-orchestrator",
  "version": "1.0.0",
  "description": "Tether Agent Dashboard — filesystem API for reports, inbox, and agents",
  "main": "server/index.js",
  "scripts": {
    "dashboard": "node server/index.js",
    "test": "jest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.1.0"
  }
}
